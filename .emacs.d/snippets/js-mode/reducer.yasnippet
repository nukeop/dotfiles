# -*- mode: snippet -*-
# name: Redux.reducer
# key: reducer
# --

import {

} from $1;

const initialState = {

};

export default function `(capitalize-first(file-name-nondirectory (file-name-sans-extension (buffer-file-name))))`Reducer(state=initialState, action) {
  switch(action.type) {
    default:
    return state;
  }
}